const accountId = 123434;  //--values can not be changed
let acccountEmail = "adityakanase@gmail.com"; //access only inside brackets
var accountPassowart = "pass"; // access anywhere in file (security issue) so not in use
accountCity = "shenoli";// --> can be possible to decleare but not recommencded


let accountState;
console.log(accountState);

// console.log(accountCity);

/*
this is multiline comment

*/

//below line will crete a table and give the indexing from 0 for each variable 
console.table([accountId,acccountEmail,accountCity]);
